// Content model types — AEM-independent interface
// The AEM adapter in ./aem/ implements these using src/lib/aem/types.ts
export type { ArticleModel, ArticleListResponse, ArticleByPathResponse, SlingModelResponse } from "@/lib/aem/types";
